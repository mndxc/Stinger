#include "layout.h"
#include "esp_log.h"

static const char *TAG = "LAYOUT";

esp_err_t layout_render_splash(void) {
    ESP_LOGI(TAG, "Rendering Splash Screen...");
    // TODO: Implement splash screen rendering
    return ESP_OK;
}
